type Room = {
  id: string;
  name: string;
  description: string;
};

type User = {
  id: string;
  name: string;
};

type Message = {
  id: string;
  content: string;
  sender: string;
  date: Date;
};
